services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: visitor
      POSTGRES_PASSWORD: visitor
      POSTGRES_DB: visitor
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
